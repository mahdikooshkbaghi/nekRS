# Cylinder mesh placeholder

The final handout requires a checked-in, body-fitted `.re2` generated with a
supported NekRS route. This directory intentionally contains no unverified
binary mesh: the callback-based adapter currently stops before a cylinder run.
Do not use `mv_cyl` as a substitute; it is a moving-geometry low-Mach case.
